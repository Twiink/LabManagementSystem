<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.labmanagementsystembackend.mapper.NotificationMapper">

    <resultMap id="NotificationMap" type="com.example.labmanagementsystembackend.domain.entity.Notification">
        <id column="id" property="id" />
        <result column="user_id" property="userId" />
        <result column="type" property="type" />
        <result column="content" property="content" />
        <result column="status" property="status" />
        <result column="created_at" property="createdAt" />
    </resultMap>

    <insert id="insertNotification" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO notifications (user_id, type, content, status)
        VALUES (#{userId}, #{type}, #{content}, #{status})
    </insert>

    <select id="findById" resultMap="NotificationMap">
        SELECT * FROM notifications WHERE id = #{id}
    </select>

    <select id="findNotifications" resultMap="NotificationMap">
        SELECT * FROM notifications
        <where>
            <if test="status != null and status != ''">AND status = #{status}</if>
        </where>
        ORDER BY id DESC
        LIMIT #{pageSize} OFFSET #{offset}
    </select>

    <select id="countNotifications" resultType="long">
        SELECT COUNT(1) FROM notifications
        <where>
            <if test="status != null and status != ''">AND status = #{status}</if>
        </where>
    </select>

    <update id="markRead">
        UPDATE notifications SET status = 'READ' WHERE id = #{id}
    </update>
</mapper>
