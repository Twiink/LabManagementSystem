<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.labmanagementsystembackend.mapper.RuleConfigMapper">

    <resultMap id="RuleConfigMap" type="com.example.labmanagementsystembackend.domain.entity.RuleConfig">
        <id column="id" property="id" />
        <result column="key_name" property="keyName" />
        <result column="value" property="value" />
        <result column="description" property="description" />
        <result column="updated_at" property="updatedAt" />
    </resultMap>

    <select id="findAll" resultMap="RuleConfigMap">
        SELECT * FROM rule_configs ORDER BY key_name ASC
    </select>

    <select id="findByKey" resultMap="RuleConfigMap">
        SELECT * FROM rule_configs WHERE key_name = #{keyName}
    </select>

    <insert id="upsertRule" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO rule_configs (key_name, value, description)
        VALUES (#{keyName}, #{value}, #{description})
        ON DUPLICATE KEY UPDATE value = VALUES(value), description = VALUES(description)
    </insert>
</mapper>
