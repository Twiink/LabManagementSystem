<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.labmanagementsystembackend.mapper.UserMapper">

    <resultMap id="UserMap" type="com.example.labmanagementsystembackend.domain.entity.User">
        <id column="id" property="id" />
        <result column="name" property="name" />
        <result column="email" property="email" />
        <result column="phone" property="phone" />
        <result column="role" property="role" />
        <result column="status" property="status" />
        <result column="password_hash" property="passwordHash" />
        <result column="is_deleted" property="isDeleted" />
        <result column="created_at" property="createdAt" />
        <result column="updated_at" property="updatedAt" />
    </resultMap>

    <select id="findById" resultMap="UserMap">
        SELECT * FROM users WHERE id = #{id} AND is_deleted = 0
    </select>

    <select id="findByName" resultMap="UserMap">
        SELECT * FROM users WHERE name = #{name} AND is_deleted = 0 LIMIT 1
    </select>

    <select id="findByEmail" resultMap="UserMap">
        SELECT * FROM users WHERE email = #{email} AND is_deleted = 0 LIMIT 1
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (name, email, phone, role, status, password_hash, is_deleted, created_at, updated_at)
        VALUES (#{name}, #{email}, #{phone}, #{role}, #{status}, #{passwordHash}, #{isDeleted}, #{createdAt}, #{updatedAt})
    </insert>

    <select id="findUsers" resultMap="UserMap">
        SELECT * FROM users
        <where>
            AND is_deleted = 0
            <if test="role != null and role != ''">AND role = #{role}</if>
            <if test="status != null and status != ''">AND status = #{status}</if>
        </where>
        ORDER BY id DESC
        LIMIT #{pageSize} OFFSET #{offset}
    </select>

    <select id="countUsers" resultType="long">
        SELECT COUNT(1) FROM users
        <where>
            AND is_deleted = 0
            <if test="role != null and role != ''">AND role = #{role}</if>
            <if test="status != null and status != ''">AND status = #{status}</if>
        </where>
    </select>

    <update id="updateUser">
        UPDATE users
        SET name = #{name},
            email = #{email},
            phone = #{phone},
            role = #{role},
            status = #{status},
            password_hash = #{passwordHash},
            updated_at = NOW()
        WHERE id = #{id} AND is_deleted = 0
    </update>
</mapper>
