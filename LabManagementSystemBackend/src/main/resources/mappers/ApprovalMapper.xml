<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.labmanagementsystembackend.mapper.ApprovalMapper">

    <insert id="insertApproval" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO approvals (reservation_id, action, reason, operator_id)
        VALUES (#{reservationId}, #{action}, #{reason}, #{operatorId})
    </insert>
</mapper>
